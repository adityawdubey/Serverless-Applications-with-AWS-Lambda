[pytest]
# importlib mode keeps pytest from prepending the rootdir to sys.path, so the
# `pythonpath` order below is honoured: the handler folder first (so `import
# handler` resolves to the Lambda code) then the project root (for `stacks`).
addopts = --import-mode=importlib
pythonpath = lambda_functions/order_service .
testpaths = tests
