[pytest]
# importlib mode keeps pytest from prepending the rootdir to sys.path, so the
# `pythonpath` order below is honoured and `import app` resolves to src/app.py
# (the Lambda handler) rather than the root app.py (the CDK entry script).
addopts = --import-mode=importlib
pythonpath = src .
testpaths = tests
